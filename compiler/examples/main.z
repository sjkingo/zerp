# this example shows variable declarations, assignment, addition
# operator, and the print statement
begin main()
    var i : integer;
    var j : integer;
    var r : integer;
    i := 1;
    j := 2;
end
